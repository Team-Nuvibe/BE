

# HTTP 서버, 인증서 발급용
server {
    listen 80;
    server_name api.nuvibe.com nuvibe.com;

    # SSL 인증서 발급/갱신
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # 백엔드 프록시 설정
    location / {
        proxy_pass http://app:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 타임아웃 설정
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # 헬스체크 엔드포인트
    location /actuator/health {
        proxy_pass http://app:8080/actuator/health;
        access_log off;
    }
}

# ========================================
# HTTPS 서버 (2단계: SSL 인증서 발급 후 주석 제거)
# ========================================
#
# [주의사항]
# 1. 반드시 위의 certbot 명령어로 두 도메인을 함께 발급받아야 함
# 2. 인증서 발급 성공 후 아래 주석을 제거하세요
# 3. 최종 배포 시 HTTP → HTTPS 리다이렉트 활성화 권장 (맨 아래 참고)
#
# # HTTPS 서버 (API 도메인)
# server {
#     listen 443 ssl http2;
#     server_name api.nuvibe.com;
#
#     # SSL 인증서 (api.nuvibe.com + nuvibe.com 포함된 SAN 인증서)
#     ssl_certificate /etc/letsencrypt/live/api.nuvibe.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.nuvibe.com/privkey.pem;
#
#     # SSL 설정 (보안 강화)
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_prefer_server_ciphers on;
#     ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384';
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#
#     # 클라이언트 요청 크기 제한
#     client_max_body_size 10M;
#
#     # 백엔드 프록시 설정
#     location / {
#         proxy_pass http://app:8080;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#
#         # 타임아웃 설정
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 60s;
#         proxy_read_timeout 60s;
#     }
#
#     # 헬스체크 엔드포인트
#     location /actuator/health {
#         proxy_pass http://app:8080/actuator/health;
#         access_log off;
#     }
# }
#
# # HTTPS 서버 (루트 도메인 → API 리다이렉트)
# server {
#     listen 443 ssl http2;
#     server_name nuvibe.com;
#
#     # 동일한 인증서 사용 (SAN 방식으로 함께 발급받았음)
#     ssl_certificate /etc/letsencrypt/live/api.nuvibe.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.nuvibe.com/privkey.pem;
#
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_prefer_server_ciphers on;
#     ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384';
#
#     # API 서버로 리다이렉트
#     location / {
#         return 301 https://api.nuvibe.com$request_uri;
#     }
# }

# ========================================
# [최종 배포 시 권장] HTTP → HTTPS 강제 리다이렉트
# ========================================
#
# HTTPS 활성화 후, HTTP 서버 블록을 아래와 같이 수정하면
# 더욱 안전한 프로덕션 환경이 됩니다:
#
# server {
#     listen 80;
#     server_name api.nuvibe.com nuvibe.com;
#
#     # Certbot 갱신용 (반드시 유지)
#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }
#
#     # 나머지는 모두 HTTPS로 리다이렉트 (보안)
#     location / {
#         return 301 https://$host$request_uri;
#     }
# }
